<a href="javascript:void(0)" class="btn btn-primary d-inline-flex add-button" data-bs-toggle="modal" data-bs-target="#form-modal" data-title="{{ title }}">
  <em class="icon ni ni-{{ icon || 'plus' }}"></em>
  <span class="d-none d-md-block">{{ label || 'Tambah' }}</span>
</a>
